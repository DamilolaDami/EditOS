import CoreGraphics

/// Shared sizing constants so the track header column and track rows stay aligned.
enum TimelineMetrics {
    static let rulerHeight: CGFloat = 40
    static let trackHeight: CGFloat = 56
    static let trackSpacing: CGFloat = 2
    static let headerWidth: CGFloat = 168
    static let basePixelsPerSecond: CGFloat = 60
}
