import XCTest

/// Placeholder that just keeps the bundle compiling. Real tests live in
/// `TimelineTests`, `EditorViewModelTests`, and `CompositionBuilderTests`.
final class EditOSTests: XCTestCase {
    func testBundleLoads() {
        XCTAssertTrue(true)
    }
}
